---
archived: ~
categories: Perl，Orion，Performance，效能，Apache
dependencies: '*.md.zh-TW'
keywords: perl，dylan，static，靜態，方法，lookup，查詢，compile，編譯，sealed，apache，mod_perl，performance，效能
published: ~
status: 已封存
title: Perl 7 功能要求：輸入詞彙的密封子
---

<div class="right">

![日本人無修正](../images/sunstarstaronly.png).

</div>

## 問題

{# lede #}Perl 5 的 OO 程式實際執行方法查尋比直接、具名子常式呼叫多出 50% 的效能超載{# lede #}.

## 最初的解決方案：Doug MacEachern 的方法查找優化

Doug 是 mod_perl 專案的創作者，回傳在 90s 中，因此顯然是高效能的 Perl。他的許多貢獻之一[p5p](https://lists.perl.org/list/perl5-porters.html) 是透過使用方法 + <code>，將 OO 方法查詢負荷的效能降低一半&#64;ISA</code> 階層快取，讓程式實際執行物件方法查尋 mod_perl 物件 (例如`Apache2::RequestRec` 盡可能簡化。但它只讓我們走半路。

這不是處理呼叫的問題`C 結構` 取得集存取器方法&mdash; 有許多 mod_perl API 的常見情況。Perl 在 httpd 的程式實際執行方法呼叫查尋罰款`結構 request_rec *`，該 mod_perl 會透過`Apache2::RequestRec` 模組的執行順序與呼叫的完整執行順序相同。對於讓數百萬個 XS 方法呼叫秒的 mod_perl 支持網站而言，這是非常浪費寶貴的 CPU 週期。

什麼[道格在尋找](https://www.perl.com/pub/2000/06/dougpatch.html/) 這是告訴 Perl 5 在編譯階段執行方法查詢的一種方法，它與具名子常式呼叫的方式。每次道格試過，他都擊中社會或技術本質的障礙。也許是時候以 Perl 7 的冒險，在這個想法中再次通過。

## [指標命令檔]({{snippetA.pretty_uri}}).

[snippet:repo=SunStarSys/sealed:path=t/bench.pl:lang=perl]

## 指標結果

```perl
1..3
sealed: compiling main->foo lookup.
sub sealed :sealed {
    use warnings;
    use strict;
    $y->foo:compiled;
}
sealed: compiling Benchmark->cmpthese lookup.
sealed: compiling Foo->foo lookup.
sealed: compiling main->foo lookup.
sealed: compiling Foo->bar lookup.
sealed: tweak() aborted: sealed: invalid lookup: Foo->bar - did you forget to 'use Foo' first?
sub __ANON__ :sealed {
    use warnings;
    use strict;
    my Foo $b = $a;
    $inner->foo($b->foo:compiled($inner->bar, $inner, $bench->cmpthese:compiled));
    $a = $inner;
    $a->foo:compiled;
    $b->bar;
}
sealed: compiling main->bar lookup.
sub also_sealed :sealed {
    use warnings;
    use strict;
    my main $a = shift();
    if ($a) {
        my Benchmark $bench = 'Benchmark';
        my $inner = $a;
        return sub {
            my Foo $b = $a;
            $inner->foo($b->foo:compiled($inner->bar, $inner, $bench->cmpthese:compiled));
            $a = $inner;
            $a->foo:compiled;
            $b->bar;
        }
        ;
    }
    $a->bar:compiled;
}
sealed: compiling main->_foo lookup.
sub reentrant :sealed {
    use warnings;
    use strict;
    my main $b = shift();
    (local our(@Q)) = 1;
    my $c = $b->_foo:compiled;
}
sealed: compiling main->foo lookup.
sealed: compiling main->bar lookup.
sealed: compiling main->reentrant lookup.
sub sealed2 :sealed {
    use warnings;
    use strict;
    my main $obj = 'main';
    foreach $_ (1 .. 3) {
        $obj->foo:compiled;
        $obj->bar:compiled;
        $obj->reentrant:compiled;
    }
}
sealed: compiling main->reentrant lookup.
sealed: compiling main->bar lookup.
sub _foo :sealed {
    package Foo;
    use warnings;
    use strict;
    my main $x = shift();
    $n++ ? $x->bar:compiled : $x->reentrant:compiled;
}
ok 1
             Rate  class method   anon   func sealed
class  16129032/s     --    -4%   -26%   -33%   -36%
method 16806723/s     4%     --   -23%   -30%   -34%
anon   21739130/s    35%    29%     --   -10%   -14%
func   24096386/s    49%    43%    11%     --    -5%
sealed 25316456/s    57%    51%    16%     5%     --
ok 2
           Rate method sealed
method 546448/s     --   -17%
sealed 662252/s    21%     --
ok 3
```

## 提議的 Perl 7 解決方案：`：已密封` 打字語彙的子常式

範例程式碼：

```perl
use v5.38;
use Apache2::RequestRec;

sub handler :Sealed (Apache2::RequestRec $r) {
  $r->content_type("text/html"); #compile time method lookup
}
```

## 生產 - 質量，強健性保存 v5.28+ 原型：sealed.pm {{facts.releases.sealed.tag}} (CPAN).

5.30 以上版本的編譯指示可從以下位置取得：`sealed.pm` Pod 應執行 mod_perl2 (含 ithreads 和 httpd-2.4 (含事件 mpm)，而不是 **any** 比例的 segfault。測試日期`Solaris 11.4` 與`烏本圖 22.04` 在 amd64 上。

為了有趣，請試試看[猴修補]({{snippetB.pretty_uri}}) 至`ModPerl::RegistryCooker`:

[snippet:repo=SunStarSys/sealed:path=lib/ModPerl/RegistryCookerSealed.pm:lang=apache:lines=86-92]

它會啟用下列項目的效果：`子處理程式 ：密封的 {script go here}` 在您所有的`ModPerl：：登錄` 文稿，類似的東西[這一個](https://github.com/SunStarSys/sealed/blob/master/enquiry.pl).

```shell
~/src/cms% h2load -n 100000 -c 1000 -m 100 -t 10 http://localhost/perl-script/enquiry.pl\?lang=.es
starting benchmark...
spawning thread #0: 100 total client(s). 10000 total requests
spawning thread #1: 100 total client(s). 10000 total requests
spawning thread #2: 100 total client(s). 10000 total requests
spawning thread #3: 100 total client(s). 10000 total requests
spawning thread #4: 100 total client(s). 10000 total requests
spawning thread #5: 100 total client(s). 10000 total requests
spawning thread #6: 100 total client(s). 10000 total requests
spawning thread #7: 100 total client(s). 10000 total requests
spawning thread #8: 100 total client(s). 10000 total requests
spawning thread #9: 100 total client(s). 10000 total requests
Application protocol: h2c
progress: 10% done
progress: 20% done
progress: 30% done
progress: 40% done
progress: 50% done
progress: 60% done
progress: 70% done
progress: 80% done
progress: 90% done
progress: 100% done

finished in 13.07s, 7652.14 req/s, 11.83MB/s
requests: 100000 total, 100000 started, 100000 done, 100000 succeeded, 0 failed, 0 errored, 0 timeout
status codes: 100000 2xx, 0 3xx, 0 4xx, 0 5xx
traffic: 154.61MB (162119955) total, 566.39KB (579980) headers (space savings 95.47%), 152.30MB (159700000) data
                       min         max         mean         sd        +/- sd
time for request:     5.74ms      12.77s       6.39s       3.61s    58.14%
time for connect:      304us    293.01ms     70.17ms     76.83ms    74.80%
time to 1st byte:     7.86ms       7.87s       3.33s       1.82s    50.40%
req/s           :       7.71      248.17       19.60       28.07    92.70%
```

請參閱<https://github.com/SunStarSys/sealed/blob/master/lib/sealed.pm>。尋找`t/bench.pl` 在上層目錄中。

這將允許 Perl 5 執行範例代碼`content_type` 在編譯階段進行方法查詢，不會造成任何回溯比較問題或獲得的 CPAN 程式碼器，因為此功能會針對應用程式開發人員進行目標。不可繼承的 OO 模組作者。

這個「成就」的想法是從[狄倫](https://jim.studt.net/dirm/interim-5.html). [閱讀此文章](https://www.complang.tuwien.ac.at/gergo/papers/load_attr.pdf) CPython 從十年前開始的努力。

<!-- $Date: 2026-01-08 19:55:12 -0700 (Thu, 08 Jan 2026) $ $Author: joe $ $Revision: 27387 $ -->
