Title: Stochastic Trace Formula for Closed, Negatively Curved Manifolds
Dependencies: *.md.en
Status: draft
Categories: Math
Keywords: weiner measure,riemann surfaces,probability,geometry

[TOC]

{# lede #}My *1997 Ph.D. thesis* as a blog entry.{# lede #}

## There is only One n-Dimensional Wiener Measure μ

## Piecewise Linear Approximations to Brownian Motion

## The Development Map

## Cameron-Martin Formula

## Heat Kernels as Radon-Nicodym Derivatives

## Stochastic Trace Formula on Negatively Curved Manifolds

### Notation

$$M$$ is a negatively curved $$\dim=n$$ closed Riemannian manifold with metric $$g$$ and (positive-definite) Laplace-Beltrami Operator $$\Delta$$.

$$\mu$$ is n-dimensional Wiener Measure restricted to the pull-back of continuous loop space $$\Omega_t(M)$$, via the inverse of the measure-preserving development map $$DM$$.

$$\Omega_t^0$$ is the space of continuous contractible loops on $$M$$.

$$\Omega_t[\gamma]$$ is the space of continuous loops on $$M$$ homotopic to $$\gamma$$.

$$\Omega_t^0[\gamma]$$ is the space of continuous contractible loops on $$M$$ written as offset loops homotopic to $$\gamma$$.

```math
\begin{aligned}
Z_{-\Delta/2}(t) &:= \mu(DM^{-1}\Omega_t) = \mu(DM^{-1}\Omega^0_t) + \sum_{\set{\text{closed geodesics }\gamma}} \mu(DM^{-1}\Omega_t[\gamma]) \\

\mu(DM^{-1}\Omega_t^0) &= (2\pi t)^{-n/2}(vol(M) + t/6\int_M K(x)\sqrt{g} dx + O(t^2))\space \text{ by McKean-Singer}\\

\mu(DM^{-1}\Omega_t[\gamma]) &= e^{-\ell(\gamma)^2/2t}\mu(e^{\ell(\gamma)x^1(\tau)}DM^{-1}\Omega_t^0[\gamma])\space\text{ by Cameron-Martin}\\

\frac{d\mu(e^{-\ell(\gamma)x^1(\tau)}
DM^{-1}\Omega^0_t[\gamma])}{dx^1\dots dx^n}\vert_{\vec{x}(\tau)+\gamma(t)} &= \frac{e^{-\bra{|1-P_{\vec{x}(\tau)+\gamma(t)}|\vec{x}(\tau)}\ket{\vec{x}(\tau)}/2t}}{(2 \pi t)^{(n+1)/2}}d\tau\\

\mu(DM^{-1}\Omega_t[\gamma]) &= \frac{e^{\ell(\gamma)^2/2t}}{\sqrt{2\pi t}} \int_{DM^{-1}(\Omega^0_t[\gamma])}\frac{e^{-\bra{|1-P_{\vec{x}(\tau)+\gamma(t)}|\vec{x}}\ket{\vec{x}(\tau)}/2t}}{(2 \pi t)^{n/2}}dx^1 ... dx^nd\tau\\

\mu(DM^{-1}\Omega_t[\gamma]) &\approx \frac{e^{\ell(\gamma)^2/2t}}{\sqrt{2\pi t}} \int_{\Reals^n\oplus\Z/\ell(\gamma_0)\Z}\frac{e^{-\bra{|1-P_{\gamma(t)}|\vec{x}}\ket{\vec{x}}/2t}}{(2 \pi t)^{n/2}}dx^1 ... dx^nd\tau\\

&=\frac{e^{-\ell(\gamma)^2/2t}\ell(\gamma_0)}{\sqrt{2\pi t\det{|I-P_{\gamma(t)}|}}}

\end{aligned}
```

## Approximation and the Selberg Trace Formula

In the $$\dim=2$$ constant curvature case,

```math
\begin{aligned}
P_{\gamma(t)} &=
\begin{pmatrix}
e^{\ell(\gamma)} && 0 \\
0 && e^{-\ell(\gamma)}
\end{pmatrix} \\

\implies \\

\det|I-P_{\gamma(t)}| &= (e^{\ell(\gamma)} - 1)(1 - e^{-\ell(\gamma)})\\
&= e^{\ell(\gamma)}(1-e^{\ell(\gamma)})^2

\end{aligned}
```

and the approximation $$\approx$$ line in Equation (1) is exact:

```math
\begin{aligned}
\mu(DM^{-1}\Omega_t[\gamma]) &= \frac{e^{-\ell(\gamma)^2/2t}\ell(\gamma_0)e^{-\ell(\gamma)/2}}{\sqrt{2 \pi t}(1-e^{-\ell(\gamma)})}\\

\gamma = k\gamma_0 \implies \\

&=\frac{e^{-k^2\ell(\gamma_0)^2/2t}\ell(\gamma_0)}{2\sqrt{2\pi t}\sinh k\ell(\gamma_0)/2}\\

\end{aligned}
```

<style type="text/css">
  /* automatic line numbering kludge */
  .katex-display { display: table; width: 100% }
  .katex-display>.katex { display: table-cell }
  body { counter-reset: eqno }
  .katex-display::before, .katex-display::after {
    width: 5%;
    display: table-cell;
    text-align: right;
	vertical-align: middle;
  }
  .katex-display::after {
    counter-increment: eqno;
	content: "(" counter(eqno) ")";
  }
  .eqno::after {
    content: "(" counter(eqno) ")";
  }
</style>


<!-- $Date$ $Author$ $Revision$ -->
