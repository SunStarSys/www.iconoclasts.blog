Title: Microsoft Considered Harmful
Keywords: markdown,Microsoft,Github,corrupt,embrace,extend,sycophant
status: verified=39671
Dependencies: *.md.en
Categories: Orion

<div class="right">

![Embrace and Extend Evil Empire](markdown-is-not-latex.page/microsoft)

</div>

## Markdown's Origins

{# lede #}The point of [John Gruber’s Markdown syntax spec](https://daringfireball.net/projects/markdown/syntax) was that it was easy to work with{# lede #}; not just from an authoring standpoint, but from an implementer’s perspective as well.  $$\LaTeX$$, `MathML`, and `HTML5` are not that.

## :fa-github: GitHub Flavored Markdown (GFM)

GFM stayed true to that specification's design goals right up until [Microsoft bastardized the isolated dollar sign to conditionally convert it into a $$\LaTeX$$ token](https://docs.github.com/en/get-started/writing-on-github/working-with-advanced-formatting/writing-mathematical-expressions#writing-inline-expressions) because of some antiquated, ugly crapware called MathJax that had nothing to do with Markdown, and the fact that these assholes don't want to promote $$\KaTeX$$ without a bribe from the Khan Academy.

## Embrace and Extend Redux

Of course this was entirely intentional, because [:editormd-logo: world-class engineering teams](http://editor.md.ipandao.com) had settled on $$\KaTeX$$ with double-dollar token boundaries for inline math, and fenced math blocks for block $$\KaTeX$$ processing **before 2020**.

The point here is that a 2-character delimiter that rarely occurs in typical prose is the normal ideal for situations like this. Had Microsoft stuck to only supporting \$\` ... \`\$ I would not be writing this article in 2026, because that choice meets the normal ideal conditions for faithful introduction of inline math expressions in a Markdown spec.

It is now an ambiguous disaster of engineering regex defects every time an author/implementer has to guess what the parser is supposed to do with sequences of isolated dollar signs occurring within a single paragraph of markdown prose, as when discussing two prices, say `$`1.50, and `$`2.00.  Good luck with that (is the `$` a $$\LaTeX$$ token, or is it a currency prefix?) if you are doing a bulk GFM conversion of legacy document formats to a non-GitHub platform. It will only "work" when rendered in Visual Studio.

Most disgustingly, the embrace and extend evil empire has an army of F/OSS free labor slaves to serve as syncophantic thought police about their efforts to pilfer the labor of others for their own market gains via AI.

## Fuck Microsoft

Long story short, GFM is no longer an industry spec, but a proprietary vendor format no different from the Wikimedia garbage they copied it from.

[Orion](https://www.sunstarsys.com/orion/features) **does not, and will never, use newer GFM parsers that pretend to track this dirty misfeature**.

Instead, we rely on an internal fork of @markedjs/marked and will continue to do so for the foreseeable future.

<!-- $Date$ $Author$ $Revision$ -->
